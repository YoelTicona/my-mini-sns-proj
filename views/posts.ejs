<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Posts - Mini SNS</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />
  <link rel="stylesheet" href="css/post.css" />
  <link rel="stylesheet" href="css/global.css" />
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body class="bg-light">
  
  <!-- Header include (barra de navegación u otra estructura superior) -->
  <h1 id="header">📖 Recent Posts</h1>
  <main>
    <%- include("header") %>
  <div class="container py-5">
    <h1 class="text-center text-primary mb-4">
      <i class="bi bi-card-list"></i> Recent Posts
    </h1>

    <!-- Listado de posts -->
    <div class="row g-4">
      <% posts.forEach(function(post) { %>
        <div class="col-12">
          <%- include("feed", { post: post }) %>
        </div>
      <% }); %>
    </div>
  </div>

  <!-- Footer include (pie de página o enlaces) -->
  <%- include("footer") %>

  <!-- JavaScript para interacciones con los posts (like, comentar, etc.) -->
  <script src="js/posts.js"></script>
  </main>
</body>
</html>
